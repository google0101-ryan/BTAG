__attribute__((noreturn)) void stage2()
{
    // We need to read the CFG file off the disk, present options to the user,
    // load the desired OS with the desired protocol, and then hand off control.
    // For now, we loop
    for (;;);
}